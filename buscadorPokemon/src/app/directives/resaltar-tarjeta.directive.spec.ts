import { ResaltarTarjetaDirective } from './resaltar-tarjeta.directive';

describe('ResaltarTarjetaDirective', () => {
  it('should create an instance', () => {
    const directive = new ResaltarTarjetaDirective();
    expect(directive).toBeTruthy();
  });
});
